const userData = require('./users');
const caseData = require('./cases');
const intakeData = require('./parseIntake');

module.exports = {
    users: userData,
    cases: caseData,
    intake: intakeData
}